import { getById } from "./helpers.js";
const productsRow = getById("products-row");
const categoryDropdown = getById("category-dropdown");
const propertyDropdown = getById("property-dropdown");
const orderDropdown = getById("order-dropdown");
const modalDialog = getById("modal-dialog");
const userDialogBox = getById("user-dialog-box");
const pagination = getById("pagination");
const adminPanel = getById("admin-panel");
const panelSettings = getById("panel-settings");
export {
  productsRow,
  categoryDropdown,
  propertyDropdown,
  orderDropdown,
  modalDialog,
  userDialogBox,
  pagination,
  adminPanel,
  panelSettings,
};
